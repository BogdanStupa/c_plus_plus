#include <iostream>
#include <array>

template <typename T, size_t N>
class array {
    T a[N];
public:

};


template <typename T, T* P>
struct MyStruct {
    
};

int main() {
    std::array<int, 10> a;
    
    return 0;
}